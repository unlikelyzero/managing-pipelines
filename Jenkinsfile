pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo 'Build'
      }
    }
    stage('Unit Tests') {
      steps {
        echo 'Run Unit Tests'
      }
    }
    stage('SHRUG') {
      steps {
        echo 'More than just a shrug'
      }
    }
  }
  environment {
    test = 'test'
  }
}
